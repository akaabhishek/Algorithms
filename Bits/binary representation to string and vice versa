int main()
{
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL); 
    int n=5;
    string dec = bitset<4>(n).to_string();
    // reverse(dec.begin(), dec.end());
    int x=bitset<32>(dec).to_ulong();
    cout<<x;


return 0;
}
