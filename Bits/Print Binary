#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL); 

    int n=(1<<31)-1;
    for(int i=31;i>=0;--i){
        cout<<((n>>i)&1);
    }

return 0;
}
